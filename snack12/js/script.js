// Dare la possibilità di inserire due parole. Verificare tramite una funzione che le due parole abbiano la stessa lunghezza. 
// Se hanno la stessa lunghezza, stamparle entrambe altrimenti stampare la più lunga delle due.

// function wordLength (wordA, wordB) {
//     if (wordA.length > wordB.length) {
//         return wordA;
//     } else if (wordB.length > wordA.length) {
//         return wordB;
//     } else {
//         return wordA, wordB;
//     }
// }   

//SBAGLIATOOOOO

const userWordA = prompt('Type a word');
const userWordB = prompt('Type another word');

if (wordLength === true){

}

function wordLength (wordA, wordB) {
    if (wordA.length == WebGLTransformFeedback.length){
        return true;
    }
}